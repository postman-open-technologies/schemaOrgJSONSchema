{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/FoodEstablishment.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Food Establishment",
  "media": {
    "type": "application/json;profile=https://schema.org/FoodEstablishment"
  },
  "type": "object",
  "properties": {
    "acceptsReservations": {
      "$ref": "#/$defs/acceptsReservations"
    },
    "hasMenu": {
      "oneOf": [
        {
          "$ref": "#/$defs/hasMenu"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/hasMenu"
          }
        }
      ]
    },
    "servesCuisine": {
      "$ref": "#/$defs/servesCuisine"
    },
    "starRating": {
      "$ref": "Rating.json#/$defs/possibleRefArray"
    }
  },
  "$defs": {
    "arrayOfFoodEstablishments": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "acceptsReservations": {
      "title": "Accepts Reservations",
      "description": "Indicates whether a FoodEstablishment accepts reservations. Values can be Boolean, an URL at which reservations can be made or (for backwards compatibility) the strings Yes or No.",
      "oneOf": [
        {
          "type": "boolean"
        },
        {
          "type": "string",
          "format": "uri"
        }
      ]
    },
    "hasMenu": {
      "title": "Has Menu",
      "description": "Either the actual menu as a structured representation, as text, or a URL of the menu.\n                 Supersedes \n                \n    \n    \n    menu.",
      "anyOf": [
        {
          "$ref": "Menu.json#/$defs/possibleRef"
        },
        {
          "type": "string"
        },
        {
          "type": "string",
          "format": "uri"
        }
      ]
    },
    "servesCuisine": {
      "title": "Serves Cuisine",
      "description": "The cuisine of the restaurant.",
      "type": "string"
    }
  }
}