{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/EntryPoint.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Entry Point",
  "media": {
    "type": "application/json;profile=https://schema.org/EntryPoint"
  },
  "type": "object",
  "properties": {
    "actionApplication": {
      "$ref": "SoftwareApplication.json#/$defs/possibleRefArray"
    },
    "actionPlatform": {
      "$ref": "#/$defs/actionPlatform"
    },
    "contentType": {
      "$ref": "#/$defs/contentType"
    },
    "encodingType": {
      "$ref": "#/$defs/encodingType"
    },
    "httpMethod": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/httpMethod"
      }
    },
    "urlTemplate": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/urlTemplate"
      }
    }
  },
  "$defs": {
    "arrayOfEntryPoints": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "actionPlatform": {
      "title": "Action Platform",
      "description": "The high level platform(s) where the Action can be performed for the given URL. To specify a specific application or operating system instance, use actionApplication.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "string",
          "format": "uri"
        }
      ]
    },
    "contentType": {
      "title": "Content Type",
      "description": "The supported content type(s) for an EntryPoint response.",
      "type": "string"
    },
    "encodingType": {
      "title": "Encoding Type",
      "description": "The supported encoding type(s) for an EntryPoint request.",
      "type": "string"
    },
    "httpMethod": {
      "title": "Http Method",
      "description": "An HTTP method that specifies the appropriate HTTP method for a request to an HTTP EntryPoint. Values are capitalized strings as used in HTTP.",
      "type": "string"
    },
    "urlTemplate": {
      "title": "Url Template",
      "description": "An url template (RFC6570) that will be used to construct the target of the execution of the action.",
      "type": "string"
    }
  }
}