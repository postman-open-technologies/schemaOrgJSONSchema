{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/ProductGroup.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Product Group",
  "media": {
    "type": "application/json;profile=https://schema.org/ProductGroup"
  },
  "allOf": [
    {
      "$ref": "Product.json"
    }
  ],
  "type": "object",
  "properties": {
    "hasVariant": {
      "oneOf": [
        {
          "$ref": "Product.json#/$defs/possibleRef"
        },
        {
          "type": "array",
          "items": {
            "$ref": "Product.json#/$defs/possibleRef"
          }
        }
      ]
    },
    "productGroupID": {
      "oneOf": [
        {
          "$ref": "#/$defs/productGroupID"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/productGroupID"
          }
        }
      ]
    },
    "variesBy": {
      "oneOf": [
        {
          "$ref": "#/$defs/variesBy"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/variesBy"
          }
        }
      ]
    }
  },
  "$defs": {
    "arrayOfProductGroups": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "productGroupID": {
      "title": "Product Group ID",
      "description": "Indicates a textual identifier for a ProductGroup.",
      "type": "string"
    },
    "variesBy": {
      "title": "Varies by",
      "description": "Indicates the property or properties by which the variants in a ProductGroup vary, e.g. their size, color etc. Schema.org properties can be referenced by their short name e.g. \"color\"; terms defined elsewhere can be referenced with their URIs.",
      "anyOf": [
        {
          "$ref": "DefinedTerm.json#/$defs/possibleRef"
        },
        {
          "type": "string"
        }
      ]
    }
  }
}