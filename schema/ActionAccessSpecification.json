{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/ActionAccessSpecification.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Action Access Specification",
  "media": {
    "type": "application/json;profile=https://schema.org/ActionAccessSpecification"
  },
  "type": "object",
  "properties": {
    "availabilityEnds": {
      "$ref": "#/$defs/availabilityEnds"
    },
    "availabilityStarts": {
      "$ref": "#/$defs/availabilityStarts"
    },
    "category": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/category"
      }
    },
    "eligibleRegion": {
      "$ref": "#/$defs/eligibleRegion"
    },
    "expectsAcceptanceOf": {
      "$ref": "Offer.json#/$defs/possibleRefArray"
    },
    "ineligibleRegion": {
      "$ref": "#/$defs/ineligibleRegion"
    },
    "requiresSubscription": {
      "$ref": "#/$defs/requiresSubscription"
    }
  },
  "$defs": {
    "arrayOfActionAccessSpecifications": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "availabilityEnds": {
      "title": "Availability Ends",
      "description": "The end of the availability of the product or service included in the offer.",
      "anyOf": [
        {
          "type": "string",
          "format": "time"
        },
        {
          "type": "string",
          "format": "date"
        },
        {
          "type": "string",
          "format": "date-time"
        }
      ]
    },
    "availabilityStarts": {
      "title": "Availability Starts",
      "description": "The beginning of the availability of the product or service included in the offer.",
      "anyOf": [
        {
          "type": "string",
          "format": "time"
        },
        {
          "type": "string",
          "format": "date"
        },
        {
          "type": "string",
          "format": "date-time"
        }
      ]
    },
    "category": {
      "title": "Category",
      "description": "A category for the item. Greater signs or slashes can be used to informally indicate a category hierarchy.",
      "anyOf": [
        {
          "type": "string",
          "format": "uri"
        },
        {
          "$ref": "Thing.json#/$defs/possibleRef"
        },
        {
          "type": "string"
        },
        {
          "$ref": "PhysicalActivityCategory.json#/$defs/possibleRef"
        }
      ]
    },
    "eligibleRegion": {
      "title": "Eligible Region",
      "description": "The ISO 3166-1 (ISO 3166-1 alpha-2) or ISO 3166-2 code, the place, or the GeoShape for the geo-political region(s) for which the offer or delivery charge specification is valid.\n\nSee also ineligibleRegion.",
      "anyOf": [
        {
          "$ref": "Place.json#/$defs/possibleRef"
        },
        {
          "$ref": "GeoShape.json#/$defs/possibleRef"
        },
        {
          "type": "string"
        }
      ]
    },
    "ineligibleRegion": {
      "title": "Ineligible Region",
      "description": "The ISO 3166-1 (ISO 3166-1 alpha-2) or ISO 3166-2 code, the place, or the GeoShape for the geo-political region(s) for which the offer or delivery charge specification is not valid, e.g. a region where the transaction is not allowed.\n\nSee also eligibleRegion.",
      "anyOf": [
        {
          "$ref": "Place.json#/$defs/possibleRef"
        },
        {
          "$ref": "GeoShape.json#/$defs/possibleRef"
        },
        {
          "type": "string"
        }
      ]
    },
    "requiresSubscription": {
      "title": "Requires Subscription",
      "description": "Indicates if use of the media require a subscription  (either paid or free). Allowed values are true or false (note that an earlier version had 'yes', 'no').",
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "$ref": "MediaSubscription.json#/$defs/possibleRef"
        }
      ]
    }
  }
}