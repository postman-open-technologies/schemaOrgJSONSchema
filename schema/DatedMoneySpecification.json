{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/DatedMoneySpecification.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Dated Money Specification",
  "media": {
    "type": "application/json;profile=https://schema.org/DatedMoneySpecification"
  },
  "type": "object",
  "properties": {
    "amount": {
      "$ref": "#/$defs/amount"
    },
    "currency": {
      "$ref": "#/$defs/currency"
    },
    "endDate": {
      "$ref": "#/$defs/endDate"
    },
    "startDate": {
      "$ref": "#/$defs/startDate"
    }
  },
  "$defs": {
    "arrayOfDatedMoneySpecifications": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "amount": {
      "title": "Amount",
      "description": "The amount of money.",
      "anyOf": [
        {
          "type": "number"
        },
        {
          "$ref": "MonetaryAmount.json#/$defs/possibleRef"
        }
      ]
    },
    "currency": {
      "title": "Currency",
      "description": "The currency in which the monetary amount is expressed.\n\nUse standard formats: ISO 4217 currency format e.g. \"USD\"; Ticker symbol for cryptocurrencies e.g. \"BTC\"; well known names for Local Exchange Tradings Systems (LETS) and other currency types e.g. \"Ithaca HOUR\".",
      "type": "string"
    },
    "endDate": {
      "title": "End Date",
      "description": "The end date and time of the item (in ISO 8601 date format).",
      "type": "string",
      "format": "date-time"
    },
    "startDate": {
      "title": "Start Date",
      "description": "The start date and time of the item (in ISO 8601 date format).",
      "type": "string",
      "format": "date-time"
    }
  },
  "enum": [
    "MonetaryAmount"
  ]
}