{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/Chapter.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Chapter",
  "media": {
    "type": "application/json;profile=https://schema.org/Chapter"
  },
  "allOf": [
    {
      "$ref": "CreativeWork.json"
    }
  ],
  "type": "object",
  "properties": {
    "pageEnd": {
      "$ref": "#/$defs/pageEnd"
    },
    "pageStart": {
      "$ref": "#/$defs/pageStart"
    },
    "pagination": {
      "oneOf": [
        {
          "$ref": "#/$defs/pagination"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/pagination"
          }
        }
      ]
    }
  },
  "$defs": {
    "arrayOfChapters": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "pageEnd": {
      "title": "Page End",
      "description": "The page on which the work ends; for example \"138\" or \"xvi\".",
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "string"
        }
      ]
    },
    "pageStart": {
      "title": "Page Start",
      "description": "The page on which the work starts; for example \"135\" or \"xiii\".",
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "string"
        }
      ]
    },
    "pagination": {
      "title": "Pagination",
      "description": "Any description of pages that is not separated into pageStart and pageEnd; for example, \"1-6, 9, 55\" or \"10-12, 46-49\".",
      "type": "string"
    }
  }
}