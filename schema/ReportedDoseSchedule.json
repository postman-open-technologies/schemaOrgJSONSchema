{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/ReportedDoseSchedule.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Reported Dose Schedule",
  "media": {
    "type": "application/json;profile=https://schema.org/ReportedDoseSchedule"
  },
  "allOf": [
    {
      "$ref": "DoseSchedule.json"
    }
  ],
  "$defs": {
    "arrayOfReportedDoseSchedules": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    }
  }
}