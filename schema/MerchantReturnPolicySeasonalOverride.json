{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/MerchantReturnPolicySeasonalOverride.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Merchant Return Policy Seasonal Override",
  "media": {
    "type": "application/json;profile=https://schema.org/MerchantReturnPolicySeasonalOverride"
  },
  "type": "object",
  "properties": {
    "endDate": {
      "$ref": "#/$defs/endDate"
    },
    "merchantReturnDays": {
      "oneOf": [
        {
          "$ref": "#/$defs/merchantReturnDays"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/merchantReturnDays"
          }
        }
      ]
    },
    "returnPolicyCategory": {
      "oneOf": [
        {
          "$ref": "MerchantReturnEnumeration.json#/$defs/possibleRef"
        },
        {
          "type": "array",
          "items": {
            "$ref": "MerchantReturnEnumeration.json#/$defs/possibleRef"
          }
        }
      ]
    },
    "startDate": {
      "$ref": "#/$defs/startDate"
    }
  },
  "$defs": {
    "arrayOfMerchantReturnPolicySeasonalOverrides": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "endDate": {
      "title": "End Date",
      "description": "The end date and time of the item (in ISO 8601 date format).",
      "type": "string",
      "format": "date-time"
    },
    "merchantReturnDays": {
      "title": "Merchant Return Days",
      "description": "Specifies either a fixed return date or the number of days (from the delivery date) that a product can be returned. Used when the returnPolicyCategory property is specified as MerchantReturnFiniteReturnWindow.\n                 Supersedes \n                \n    \n    \n    productReturnDays.",
      "anyOf": [
        {
          "type": "string",
          "format": "date"
        },
        {
          "type": "string",
          "format": "date-time"
        },
        {
          "type": "integer"
        }
      ]
    },
    "startDate": {
      "title": "Start Date",
      "description": "The start date and time of the item (in ISO 8601 date format).",
      "type": "string",
      "format": "date-time"
    }
  }
}