{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/DoseSchedule.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Dose Schedule",
  "media": {
    "type": "application/json;profile=https://schema.org/DoseSchedule"
  },
  "type": "object",
  "properties": {
    "doseUnit": {
      "$ref": "#/$defs/doseUnit"
    },
    "doseValue": {
      "$ref": "#/$defs/doseValue"
    },
    "frequency": {
      "$ref": "#/$defs/frequency"
    },
    "targetPopulation": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/targetPopulation"
      }
    }
  },
  "$defs": {
    "arrayOfDoseSchedules": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "doseUnit": {
      "title": "Dose Unit",
      "description": "The unit of the dose, e.g. 'mg'.",
      "type": "string"
    },
    "doseValue": {
      "title": "Dose Value",
      "description": "The value of the dose, e.g. 500.",
      "anyOf": [
        {
          "type": "number"
        },
        {
          "$ref": "QualitativeValue.json#/$defs/possibleRef"
        }
      ]
    },
    "frequency": {
      "title": "Frequency",
      "description": "How often the dose is taken, e.g. 'daily'.",
      "type": "string"
    },
    "targetPopulation": {
      "title": "Target Population",
      "description": "Characteristics of the population for which this is intended, or which typically uses it, e.g. 'adults'.",
      "type": "string"
    }
  }
}