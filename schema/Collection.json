{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/Collection.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Collection",
  "media": {
    "type": "application/json;profile=https://schema.org/Collection"
  },
  "allOf": [
    {
      "$ref": "CreativeWork.json"
    }
  ],
  "type": "object",
  "properties": {
    "collectionSize": {
      "$ref": "#/$defs/collectionSize"
    }
  },
  "$defs": {
    "arrayOfCollections": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "collectionSize": {
      "title": "Collection Size",
      "description": "The number of items in the Collection.",
      "type": "integer"
    }
  }
}