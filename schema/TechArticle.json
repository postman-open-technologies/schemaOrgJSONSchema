{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/TechArticle.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Tech Article",
  "media": {
    "type": "application/json;profile=https://schema.org/TechArticle"
  },
  "allOf": [
    {
      "$ref": "Article.json"
    }
  ],
  "type": "object",
  "properties": {
    "dependencies": {
      "$ref": "#/$defs/dependencies"
    },
    "proficiencyLevel": {
      "$ref": "#/$defs/proficiencyLevel"
    }
  },
  "$defs": {
    "arrayOfTechArticles": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "dependencies": {
      "title": "Dependencies",
      "description": "Prerequisites needed to fulfill steps in article.",
      "type": "string"
    },
    "proficiencyLevel": {
      "title": "Proficiency Level",
      "description": "Proficiency needed for this content; expected values: 'Beginner', 'Expert'.",
      "type": "string"
    }
  }
}