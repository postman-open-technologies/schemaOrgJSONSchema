{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/RepaymentSpecification.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Repayment Specification",
  "media": {
    "type": "application/json;profile=https://schema.org/RepaymentSpecification"
  },
  "allOf": [
    {
      "$ref": "StructuredValue.json"
    }
  ],
  "type": "object",
  "properties": {
    "downPayment": {
      "oneOf": [
        {
          "$ref": "#/$defs/downPayment"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/downPayment"
          }
        }
      ]
    },
    "earlyPrepaymentPenalty": {
      "$ref": "MonetaryAmount.json#/$defs/possibleRef"
    },
    "loanPaymentAmount": {
      "$ref": "MonetaryAmount.json#/$defs/possibleRef"
    },
    "loanPaymentFrequency": {
      "oneOf": [
        {
          "$ref": "#/$defs/loanPaymentFrequency"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/loanPaymentFrequency"
          }
        }
      ]
    },
    "numberOfLoanPayments": {
      "$ref": "#/$defs/numberOfLoanPayments"
    }
  },
  "$defs": {
    "arrayOfRepaymentSpecifications": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "downPayment": {
      "title": "Down Payment",
      "description": "a type of payment made in cash during the onset of the purchase of an expensive good/service. The payment typically represents only a percentage of the full purchase price.",
      "anyOf": [
        {
          "$ref": "MonetaryAmount.json#/$defs/possibleRef"
        },
        {
          "type": "number"
        }
      ]
    },
    "loanPaymentFrequency": {
      "title": "Loan Payment Frequency",
      "description": "Frequency of payments due, i.e. number of months between payments. This is defined as a frequency, i.e. the reciprocal of a period of time.",
      "type": "number"
    },
    "numberOfLoanPayments": {
      "title": "Number of Loan Payments",
      "description": "The number of payments contractually required at origination to repay the loan. For monthly paying loans this is the number of months from the contractual first payment date to the maturity date.",
      "type": "number"
    }
  }
}