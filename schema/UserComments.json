{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/UserComments.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "User Comments",
  "media": {
    "type": "application/json;profile=https://schema.org/UserComments"
  },
  "allOf": [
    {
      "$ref": "UserInteraction.json"
    }
  ],
  "type": "object",
  "properties": {
    "commentText": {
      "$ref": "#/$defs/commentText"
    },
    "commentTime": {
      "$ref": "#/$defs/commentTime"
    },
    "creator": {
      "$ref": "#/$defs/creator"
    },
    "discusses": {
      "$ref": "CreativeWork.json#/$defs/possibleRef"
    },
    "replyToUrl": {
      "type": "string",
      "format": "uri"
    }
  },
  "links": [
    {
      "rel": "https://schema.org/replyToUrl",
      "href": "{+replyToUrl}"
    }
  ],
  "$defs": {
    "arrayOfUserCommentss": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "commentText": {
      "title": "Comment Text",
      "description": "The text of the UserComment.",
      "type": "string"
    },
    "commentTime": {
      "title": "Comment Time",
      "description": "The time at which the UserComment was made.",
      "anyOf": [
        {
          "type": "string",
          "format": "date"
        },
        {
          "type": "string",
          "format": "date-time"
        }
      ]
    },
    "creator": {
      "title": "Creator",
      "description": "The creator/author of this CreativeWork. This is the same as the Author property for CreativeWork.",
      "anyOf": [
        {
          "$ref": "Person.json#/$defs/possibleRef"
        },
        {
          "$ref": "Organization.json#/$defs/possibleRef"
        }
      ]
    }
  },
  "enum": [
    "InteractionCounter"
  ]
}