{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/PathologyTest.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Pathology Test",
  "media": {
    "type": "application/json;profile=https://schema.org/PathologyTest"
  },
  "type": "object",
  "properties": {
    "tissueSample": {
      "$ref": "#/$defs/tissueSample"
    }
  },
  "$defs": {
    "arrayOfPathologyTests": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "tissueSample": {
      "title": "Tissue Sample",
      "description": "The type of tissue sample required for the test.",
      "type": "string"
    }
  }
}