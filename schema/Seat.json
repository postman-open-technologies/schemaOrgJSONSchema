{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/Seat.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Seat",
  "media": {
    "type": "application/json;profile=https://schema.org/Seat"
  },
  "allOf": [
    {
      "$ref": "Intangible.json"
    }
  ],
  "type": "object",
  "properties": {
    "seatNumber": {
      "$ref": "#/$defs/seatNumber"
    },
    "seatRow": {
      "$ref": "#/$defs/seatRow"
    },
    "seatSection": {
      "$ref": "#/$defs/seatSection"
    },
    "seatingType": {
      "$ref": "#/$defs/seatingType"
    }
  },
  "$defs": {
    "arrayOfSeats": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "seatNumber": {
      "title": "Seat Number",
      "description": "The location of the reserved seat (e.g., 27).",
      "type": "string"
    },
    "seatRow": {
      "title": "Seat Row",
      "description": "The row location of the reserved seat (e.g., B).",
      "type": "string"
    },
    "seatSection": {
      "title": "Seat Section",
      "description": "The section location of the reserved seat (e.g. Orchestra).",
      "type": "string"
    },
    "seatingType": {
      "title": "Seating Type",
      "description": "The type/class of the seat.",
      "anyOf": [
        {
          "$ref": "QualitativeValue.json#/$defs/possibleRef"
        },
        {
          "type": "string"
        }
      ]
    }
  }
}