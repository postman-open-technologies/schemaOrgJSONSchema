{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/SpeakableSpecification.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Speakable Specification",
  "media": {
    "type": "application/json;profile=https://schema.org/SpeakableSpecification"
  },
  "allOf": [
    {
      "$ref": "Intangible.json"
    }
  ],
  "type": "object",
  "properties": {
    "cssSelector": {
      "$ref": "CssSelectorType.json#/$defs/possibleRefArray"
    },
    "xpath": {
      "$ref": "XPathType.json#/$defs/possibleRefArray"
    }
  },
  "$defs": {
    "arrayOfSpeakableSpecifications": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    }
  }
}