{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/SeekToAction.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Seek to Action",
  "media": {
    "type": "application/json;profile=https://schema.org/SeekToAction"
  },
  "type": "object",
  "properties": {
    "startOffset": {
      "$ref": "#/$defs/startOffset"
    }
  },
  "$defs": {
    "arrayOfSeekToActions": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "startOffset": {
      "title": "Start Offset",
      "description": "The start time of the clip expressed as the number of seconds from the beginning of the work.",
      "anyOf": [
        {
          "$ref": "HyperTocEntry.json#/$defs/possibleRef"
        },
        {
          "type": "number"
        }
      ]
    }
  }
}