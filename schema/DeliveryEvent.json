{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/DeliveryEvent.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Delivery Event",
  "media": {
    "type": "application/json;profile=https://schema.org/DeliveryEvent"
  },
  "type": "object",
  "properties": {
    "accessCode": {
      "$ref": "#/$defs/accessCode"
    },
    "availableFrom": {
      "$ref": "#/$defs/availableFrom"
    },
    "availableThrough": {
      "$ref": "#/$defs/availableThrough"
    },
    "hasDeliveryMethod": {
      "$ref": "DeliveryMethod.json#/$defs/possibleRef"
    }
  },
  "$defs": {
    "arrayOfDeliveryEvents": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "accessCode": {
      "title": "Access Code",
      "description": "Password, PIN, or access code needed for delivery (e.g. from a locker).",
      "type": "string"
    },
    "availableFrom": {
      "title": "Available From",
      "description": "When the item is available for pickup from the store, locker, etc.",
      "type": "string",
      "format": "date-time"
    },
    "availableThrough": {
      "title": "Available Through",
      "description": "After this date, the item will no longer be available for pickup.",
      "type": "string",
      "format": "date-time"
    }
  }
}