{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/Recipe.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Recipe",
  "media": {
    "type": "application/json;profile=https://schema.org/Recipe"
  },
  "allOf": [
    {
      "$ref": "HowTo.json"
    }
  ],
  "type": "object",
  "properties": {
    "cookTime": {
      "$ref": "Duration.json#/$defs/possibleRef"
    },
    "cookingMethod": {
      "$ref": "#/$defs/cookingMethod"
    },
    "nutrition": {
      "$ref": "NutritionInformation.json#/$defs/possibleRef"
    },
    "recipeCategory": {
      "$ref": "#/$defs/recipeCategory"
    },
    "recipeCuisine": {
      "$ref": "#/$defs/recipeCuisine"
    },
    "recipeIngredient": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/recipeIngredient"
      }
    },
    "recipeInstructions": {
      "$ref": "#/$defs/recipeInstructions"
    },
    "recipeYield": {
      "$ref": "#/$defs/recipeYield"
    },
    "suitableForDiet": {
      "oneOf": [
        {
          "$ref": "RestrictedDiet.json#/$defs/possibleRef"
        },
        {
          "type": "array",
          "items": {
            "$ref": "RestrictedDiet.json#/$defs/possibleRef"
          }
        }
      ]
    }
  },
  "$defs": {
    "arrayOfRecipes": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "cookingMethod": {
      "title": "Cooking Method",
      "description": "The method of cooking, such as Frying, Steaming, ...",
      "type": "string"
    },
    "recipeCategory": {
      "title": "Recipe Category",
      "description": "The category of the recipe—for example, appetizer, entree, etc.",
      "type": "string"
    },
    "recipeCuisine": {
      "title": "Recipe Cuisine",
      "description": "The cuisine of the recipe (for example, French or Ethiopian).",
      "type": "string"
    },
    "recipeIngredient": {
      "title": "Recipe Ingredient",
      "description": "A single ingredient used in the recipe, e.g. sugar, flour or garlic.\n                 Supersedes \n                \n    \n    \n    ingredients.",
      "type": "string"
    },
    "recipeInstructions": {
      "title": "Recipe Instructions",
      "description": "A step in making the recipe, in the form of a single item (document, video, etc.) or an ordered list with HowToStep and/or HowToSection items.",
      "anyOf": [
        {
          "$ref": "CreativeWork.json#/$defs/possibleRef"
        },
        {
          "$ref": "ItemList.json#/$defs/possibleRef"
        },
        {
          "type": "string"
        }
      ]
    },
    "recipeYield": {
      "title": "Recipe Yield",
      "description": "The quantity produced by the recipe (for example, number of people served, number of servings, etc).",
      "anyOf": [
        {
          "$ref": "QuantitativeValue.json#/$defs/possibleRef"
        },
        {
          "type": "string"
        }
      ]
    }
  }
}