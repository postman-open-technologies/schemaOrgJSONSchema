{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/ExchangeRateSpecification.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Exchange Rate Specification",
  "media": {
    "type": "application/json;profile=https://schema.org/ExchangeRateSpecification"
  },
  "type": "object",
  "properties": {
    "currency": {
      "$ref": "#/$defs/currency"
    },
    "currentExchangeRate": {
      "$ref": "UnitPriceSpecification.json#/$defs/possibleRef"
    },
    "exchangeRateSpread": {
      "$ref": "#/$defs/exchangeRateSpread"
    }
  },
  "$defs": {
    "arrayOfExchangeRateSpecifications": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "currency": {
      "title": "Currency",
      "description": "The currency in which the monetary amount is expressed.\n\nUse standard formats: ISO 4217 currency format e.g. \"USD\"; Ticker symbol for cryptocurrencies e.g. \"BTC\"; well known names for Local Exchange Tradings Systems (LETS) and other currency types e.g. \"Ithaca HOUR\".",
      "type": "string"
    },
    "exchangeRateSpread": {
      "title": "Exchange Rate Spread",
      "description": "The difference between the price at which a broker or other intermediary buys and sells foreign currency.",
      "anyOf": [
        {
          "$ref": "MonetaryAmount.json#/$defs/possibleRef"
        },
        {
          "type": "number"
        }
      ]
    }
  }
}