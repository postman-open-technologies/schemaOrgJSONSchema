{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/Recommendation.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Recommendation",
  "media": {
    "type": "application/json;profile=https://schema.org/Recommendation"
  },
  "type": "object",
  "properties": {
    "category": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/category"
      }
    }
  },
  "$defs": {
    "arrayOfRecommendations": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "category": {
      "title": "Category",
      "description": "A category for the item. Greater signs or slashes can be used to informally indicate a category hierarchy.",
      "anyOf": [
        {
          "type": "string",
          "format": "uri"
        },
        {
          "$ref": "Thing.json#/$defs/possibleRef"
        },
        {
          "$ref": "PhysicalActivityCategory.json#/$defs/possibleRef"
        },
        {
          "type": "string"
        }
      ]
    }
  }
}