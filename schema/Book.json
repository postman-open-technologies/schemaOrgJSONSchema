{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/Book.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Book",
  "media": {
    "type": "application/json;profile=https://schema.org/Book"
  },
  "allOf": [
    {
      "$ref": "CreativeWork.json"
    }
  ],
  "type": "object",
  "properties": {
    "abridged": {
      "oneOf": [
        {
          "$ref": "#/$defs/abridged"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/abridged"
          }
        }
      ]
    },
    "bookEdition": {
      "$ref": "#/$defs/bookEdition"
    },
    "bookFormat": {
      "$ref": "BookFormatType.json#/$defs/possibleRef"
    },
    "illustrator": {
      "$ref": "Person.json#/$defs/possibleRef"
    },
    "isbn": {
      "$ref": "#/$defs/isbn"
    },
    "numberOfPages": {
      "$ref": "#/$defs/numberOfPages"
    }
  },
  "$defs": {
    "arrayOfBooks": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "abridged": {
      "title": "Abridged",
      "description": "Indicates whether the book is an abridged edition.",
      "type": "boolean"
    },
    "bookEdition": {
      "title": "Book Edition",
      "description": "The edition of the book.",
      "type": "string"
    },
    "isbn": {
      "title": "ISBN",
      "description": "The ISBN of the book.",
      "type": "string"
    },
    "numberOfPages": {
      "title": "Number of Pages",
      "description": "The number of pages in the book.",
      "type": "integer"
    }
  }
}