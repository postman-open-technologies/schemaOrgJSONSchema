{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/MobileApplication.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Mobile Application",
  "media": {
    "type": "application/json;profile=https://schema.org/MobileApplication"
  },
  "allOf": [
    {
      "$ref": "SoftwareApplication.json"
    }
  ],
  "type": "object",
  "properties": {
    "carrierRequirements": {
      "$ref": "#/$defs/carrierRequirements"
    }
  },
  "$defs": {
    "arrayOfMobileApplications": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "carrierRequirements": {
      "title": "Carrier Requirements",
      "description": "Specifies specific carrier(s) requirements for the application (e.g. an application may only work on a specific carrier network).",
      "type": "string"
    }
  }
}