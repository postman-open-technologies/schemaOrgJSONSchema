{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/MathSolver.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Math Solver",
  "media": {
    "type": "application/json;profile=https://schema.org/MathSolver"
  },
  "allOf": [
    {
      "$ref": "CreativeWork.json"
    }
  ],
  "type": "object",
  "properties": {
    "mathExpression": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/mathExpression"
      }
    }
  },
  "$defs": {
    "arrayOfMathSolvers": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "mathExpression": {
      "title": "Math Expression",
      "description": "A mathematical expression (e.g. 'x^2-3x=0') that may be solved for a specific variable, simplified, or transformed. This can take many formats, e.g. LaTeX, Ascii-Math, or math as you would write with a keyboard.",
      "anyOf": [
        {
          "$ref": "SolveMathAction.json#/$defs/possibleRef"
        },
        {
          "type": "string"
        }
      ]
    }
  }
}