{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/PostalCodeRangeSpecification.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Postal Code Range Specification",
  "media": {
    "type": "application/json;profile=https://schema.org/PostalCodeRangeSpecification"
  },
  "type": "object",
  "properties": {
    "postalCodeBegin": {
      "oneOf": [
        {
          "$ref": "#/$defs/postalCodeBegin"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/postalCodeBegin"
          }
        }
      ]
    },
    "postalCodeEnd": {
      "oneOf": [
        {
          "$ref": "#/$defs/postalCodeEnd"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/postalCodeEnd"
          }
        }
      ]
    }
  },
  "$defs": {
    "arrayOfPostalCodeRangeSpecifications": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "postalCodeBegin": {
      "title": "Postal Code Begin",
      "description": "First postal code in a range (included).",
      "type": "string"
    },
    "postalCodeEnd": {
      "title": "Postal Code End",
      "description": "Last postal code in the range (included). Needs to be after postalCodeBegin.",
      "type": "string"
    }
  }
}