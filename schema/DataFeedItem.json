{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/DataFeedItem.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Data Feed Item",
  "media": {
    "type": "application/json;profile=https://schema.org/DataFeedItem"
  },
  "type": "object",
  "properties": {
    "dateCreated": {
      "$ref": "#/$defs/dateCreated"
    },
    "dateDeleted": {
      "$ref": "#/$defs/dateDeleted"
    },
    "dateModified": {
      "$ref": "#/$defs/dateModified"
    },
    "item": {
      "$ref": "Thing.json#/$defs/possibleRefArray"
    }
  },
  "$defs": {
    "arrayOfDataFeedItems": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "dateCreated": {
      "title": "Date Created",
      "description": "The date on which the CreativeWork was created or the item was added to a DataFeed.",
      "anyOf": [
        {
          "type": "string",
          "format": "date-time"
        },
        {
          "type": "string",
          "format": "date"
        }
      ]
    },
    "dateDeleted": {
      "title": "Date Deleted",
      "description": "The datetime the item was removed from the DataFeed.",
      "anyOf": [
        {
          "type": "string",
          "format": "date"
        },
        {
          "type": "string",
          "format": "date-time"
        }
      ]
    },
    "dateModified": {
      "title": "Date Modified",
      "description": "The date on which the CreativeWork was most recently modified or when the item's entry was modified within a DataFeed.",
      "anyOf": [
        {
          "type": "string",
          "format": "date-time"
        },
        {
          "type": "string",
          "format": "date"
        }
      ]
    }
  }
}