{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/DrugCost.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Drug Cost",
  "media": {
    "type": "application/json;profile=https://schema.org/DrugCost"
  },
  "type": "object",
  "properties": {
    "applicableLocation": {
      "$ref": "AdministrativeArea.json#/$defs/possibleRef"
    },
    "costCategory": {
      "$ref": "DrugCostCategory.json#/$defs/possibleRef"
    },
    "costCurrency": {
      "$ref": "#/$defs/costCurrency"
    },
    "costOrigin": {
      "$ref": "#/$defs/costOrigin"
    },
    "costPerUnit": {
      "$ref": "#/$defs/costPerUnit"
    },
    "drugUnit": {
      "$ref": "#/$defs/drugUnit"
    }
  },
  "$defs": {
    "arrayOfDrugCosts": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "costCurrency": {
      "title": "Cost Currency",
      "description": "The currency (in 3-letter of the drug cost. See: http://en.wikipedia.org/wiki/ISO_4217.",
      "type": "string"
    },
    "costOrigin": {
      "title": "Cost Origin",
      "description": "Additional details to capture the origin of the cost data. For example, 'Medicare Part B'.",
      "type": "string"
    },
    "costPerUnit": {
      "title": "Cost Per Unit",
      "description": "The cost per unit of the drug.",
      "anyOf": [
        {
          "type": "number"
        },
        {
          "$ref": "QualitativeValue.json#/$defs/possibleRef"
        },
        {
          "type": "string"
        }
      ]
    },
    "drugUnit": {
      "title": "Drug Unit",
      "description": "The unit in which the drug is measured, e.g. '5 mg tablet'.",
      "type": "string"
    }
  }
}