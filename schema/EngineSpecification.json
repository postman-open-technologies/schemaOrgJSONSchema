{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/EngineSpecification.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Engine Specification",
  "media": {
    "type": "application/json;profile=https://schema.org/EngineSpecification"
  },
  "type": "object",
  "properties": {
    "engineDisplacement": {
      "$ref": "QuantitativeValue.json#/$defs/possibleRef"
    },
    "enginePower": {
      "$ref": "QuantitativeValue.json#/$defs/possibleRef"
    },
    "engineType": {
      "$ref": "#/$defs/engineType"
    },
    "fuelType": {
      "$ref": "#/$defs/fuelType"
    },
    "torque": {
      "$ref": "QuantitativeValue.json#/$defs/possibleRef"
    }
  },
  "$defs": {
    "arrayOfEngineSpecifications": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "engineType": {
      "title": "Engine Type",
      "description": "The type of engine or engines powering the vehicle.",
      "anyOf": [
        {
          "$ref": "QualitativeValue.json#/$defs/possibleRef"
        },
        {
          "type": "string"
        },
        {
          "type": "string",
          "format": "uri"
        }
      ]
    },
    "fuelType": {
      "title": "Fuel Type",
      "description": "The type of fuel suitable for the engine or engines of the vehicle. If the vehicle has only one engine, this property can be attached directly to the vehicle.",
      "anyOf": [
        {
          "$ref": "QualitativeValue.json#/$defs/possibleRef"
        },
        {
          "type": "string",
          "format": "uri"
        },
        {
          "type": "string"
        }
      ]
    }
  }
}