{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/IndividualProduct.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Individual Product",
  "media": {
    "type": "application/json;profile=https://schema.org/IndividualProduct"
  },
  "allOf": [
    {
      "$ref": "Product.json"
    }
  ],
  "type": "object",
  "properties": {
    "serialNumber": {
      "$ref": "#/$defs/serialNumber"
    }
  },
  "$defs": {
    "arrayOfIndividualProducts": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "serialNumber": {
      "title": "Serial Number",
      "description": "The serial number or any alphanumeric identifier of a particular product. When attached to an offer, it is a shortcut for the serial number of the product included in the offer.",
      "type": "string"
    }
  }
}