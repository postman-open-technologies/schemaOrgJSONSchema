{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/ChemicalSubstance.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Chemical Substance",
  "media": {
    "type": "application/json;profile=https://schema.org/ChemicalSubstance"
  },
  "allOf": [
    {
      "$ref": "BioChemEntity.json"
    }
  ],
  "type": "object",
  "properties": {
    "chemicalComposition": {
      "$ref": "#/$defs/chemicalComposition"
    },
    "chemicalRole": {
      "$ref": "DefinedTerm.json#/$defs/possibleRefArray"
    },
    "potentialUse": {
      "oneOf": [
        {
          "$ref": "DefinedTerm.json#/$defs/possibleRef"
        },
        {
          "type": "array",
          "items": {
            "$ref": "DefinedTerm.json#/$defs/possibleRef"
          }
        }
      ]
    }
  },
  "$defs": {
    "arrayOfChemicalSubstances": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "chemicalComposition": {
      "title": "Chemical Composition",
      "description": "The chemical composition describes the identity and relative ratio of the chemical elements that make up the substance.",
      "type": "string"
    }
  }
}