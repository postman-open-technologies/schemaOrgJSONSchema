{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/WebSite.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Web Site",
  "media": {
    "type": "application/json;profile=https://schema.org/WebSite"
  },
  "type": "object",
  "properties": {
    "issn": {
      "$ref": "#/$defs/issn"
    }
  },
  "$defs": {
    "arrayOfWebSites": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "issn": {
      "title": "Issn",
      "description": "The International Standard Serial Number (ISSN) that identifies this serial publication. You can repeat this property to identify different formats of, or the linking ISSN (ISSN-L) for, this serial publication.",
      "type": "string"
    }
  }
}