{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/BroadcastChannel.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Broadcast Channel",
  "media": {
    "type": "application/json;profile=https://schema.org/BroadcastChannel"
  },
  "type": "object",
  "properties": {
    "broadcastChannelId": {
      "$ref": "#/$defs/broadcastChannelId"
    },
    "broadcastFrequency": {
      "$ref": "#/$defs/broadcastFrequency"
    },
    "broadcastServiceTier": {
      "$ref": "#/$defs/broadcastServiceTier"
    },
    "genre": {
      "$ref": "#/$defs/genre"
    },
    "inBroadcastLineup": {
      "$ref": "CableOrSatelliteService.json#/$defs/possibleRef"
    },
    "providesBroadcastService": {
      "$ref": "BroadcastService.json#/$defs/possibleRef"
    }
  },
  "$defs": {
    "arrayOfBroadcastChannels": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "broadcastChannelId": {
      "title": "Broadcast Channel Id",
      "description": "The unique address by which the BroadcastService can be identified in a provider lineup. In US, this is typically a number.",
      "type": "string"
    },
    "broadcastFrequency": {
      "title": "Broadcast Frequency",
      "description": "The frequency used for over-the-air broadcasts. Numeric values or simple ranges e.g. 87-99. In addition a shortcut idiom is supported for frequences of AM and FM radio channels, e.g. \"87 FM\".",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "$ref": "BroadcastFrequencySpecification.json#/$defs/possibleRef"
        }
      ]
    },
    "broadcastServiceTier": {
      "title": "Broadcast Service Tier",
      "description": "The type of service required to have access to the channel (e.g. Standard or Premium).",
      "type": "string"
    },
    "genre": {
      "title": "Genre",
      "description": "Genre of the creative work, broadcast channel or group.",
      "anyOf": [
        {
          "type": "string",
          "format": "uri"
        },
        {
          "type": "string"
        }
      ]
    }
  }
}