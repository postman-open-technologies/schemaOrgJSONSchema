{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/HealthPlanNetwork.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Health Plan Network",
  "media": {
    "type": "application/json;profile=https://schema.org/HealthPlanNetwork"
  },
  "type": "object",
  "properties": {
    "healthPlanCostSharing": {
      "oneOf": [
        {
          "$ref": "#/$defs/healthPlanCostSharing"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/healthPlanCostSharing"
          }
        }
      ]
    },
    "healthPlanNetworkId": {
      "oneOf": [
        {
          "$ref": "#/$defs/healthPlanNetworkId"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/healthPlanNetworkId"
          }
        }
      ]
    },
    "healthPlanNetworkTier": {
      "$ref": "#/$defs/healthPlanNetworkTier"
    }
  },
  "$defs": {
    "arrayOfHealthPlanNetworks": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "healthPlanCostSharing": {
      "title": "Health Plan Cost Sharing",
      "description": "Whether The costs to the patient for services under this network or formulary.",
      "type": "boolean"
    },
    "healthPlanNetworkId": {
      "title": "Health Plan Network Id",
      "description": "Name or unique ID of network. (Networks are often reused across different insurance plans).",
      "type": "string"
    },
    "healthPlanNetworkTier": {
      "title": "Health Plan Network Tier",
      "description": "The tier(s) for this network.",
      "type": "string"
    }
  }
}