{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/Property.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Property",
  "media": {
    "type": "application/json;profile=https://schema.org/Property"
  },
  "allOf": [
    {
      "$ref": "Intangible.json"
    }
  ],
  "type": "object",
  "properties": {
    "domainIncludes": {
      "$ref": "Class.json#/$defs/possibleRefArray"
    },
    "inverseOf": {
      "oneOf": [
        {
          "$ref": "Property.json#/$defs/possibleRef"
        },
        {
          "type": "array",
          "items": {
            "$ref": "Property.json#/$defs/possibleRef"
          }
        }
      ]
    },
    "rangeIncludes": {
      "$ref": "Class.json#/$defs/possibleRefArray"
    },
    "supersededBy": {
      "oneOf": [
        {
          "$ref": "#/$defs/supersededBy"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/supersededBy"
          }
        }
      ]
    }
  },
  "$defs": {
    "arrayOfPropertys": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "supersededBy": {
      "title": "Superseded by",
      "description": "Relates a term (i.e. a property, class or enumeration) to one that supersedes it.",
      "anyOf": [
        {
          "$ref": "Property.json#/$defs/possibleRef"
        },
        {
          "$ref": "Class.json#/$defs/possibleRef"
        },
        {
          "$ref": "Enumeration.json#/$defs/possibleRef"
        }
      ]
    }
  }
}