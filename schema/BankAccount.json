{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/BankAccount.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Bank Account",
  "media": {
    "type": "application/json;profile=https://schema.org/BankAccount"
  },
  "allOf": [
    {
      "$ref": "FinancialProduct.json"
    }
  ],
  "type": "object",
  "properties": {
    "accountMinimumInflow": {
      "$ref": "MonetaryAmount.json#/$defs/possibleRefArray"
    },
    "accountOverdraftLimit": {
      "$ref": "MonetaryAmount.json#/$defs/possibleRefArray"
    },
    "bankAccountType": {
      "$ref": "#/$defs/bankAccountType"
    }
  },
  "$defs": {
    "arrayOfBankAccounts": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "bankAccountType": {
      "title": "Bank Account Type",
      "description": "The type of a bank account.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "string",
          "format": "uri"
        }
      ]
    }
  }
}