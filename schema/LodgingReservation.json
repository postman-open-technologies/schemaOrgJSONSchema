{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/LodgingReservation.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Lodging Reservation",
  "media": {
    "type": "application/json;profile=https://schema.org/LodgingReservation"
  },
  "type": "object",
  "properties": {
    "checkinTime": {
      "$ref": "#/$defs/checkinTime"
    },
    "checkoutTime": {
      "$ref": "#/$defs/checkoutTime"
    },
    "lodgingUnitDescription": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/lodgingUnitDescription"
      }
    },
    "lodgingUnitType": {
      "oneOf": [
        {
          "$ref": "#/$defs/lodgingUnitType"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/lodgingUnitType"
          }
        }
      ]
    },
    "numAdults": {
      "$ref": "#/$defs/numAdults"
    },
    "numChildren": {
      "$ref": "#/$defs/numChildren"
    }
  },
  "$defs": {
    "arrayOfLodgingReservations": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "checkinTime": {
      "title": "Checkin Time",
      "description": "The earliest someone may check into a lodging establishment.",
      "anyOf": [
        {
          "type": "string",
          "format": "time"
        },
        {
          "type": "string",
          "format": "date-time"
        }
      ]
    },
    "checkoutTime": {
      "title": "Checkout Time",
      "description": "The latest someone may check out of a lodging establishment.",
      "anyOf": [
        {
          "type": "string",
          "format": "time"
        },
        {
          "type": "string",
          "format": "date-time"
        }
      ]
    },
    "lodgingUnitDescription": {
      "title": "Lodging Unit Description",
      "description": "A full description of the lodging unit.",
      "type": "string"
    },
    "lodgingUnitType": {
      "title": "Lodging Unit Type",
      "description": "Textual description of the unit type (including suite vs. room, size of bed, etc.).",
      "anyOf": [
        {
          "$ref": "QualitativeValue.json#/$defs/possibleRef"
        },
        {
          "type": "string"
        }
      ]
    },
    "numAdults": {
      "title": "Num Adults",
      "description": "The number of adults staying in the unit.",
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "$ref": "QuantitativeValue.json#/$defs/possibleRef"
        }
      ]
    },
    "numChildren": {
      "title": "Num Children",
      "description": "The number of children staying in the unit.",
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "$ref": "QuantitativeValue.json#/$defs/possibleRef"
        }
      ]
    }
  }
}