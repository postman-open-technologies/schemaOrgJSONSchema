{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/Rating.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Rating",
  "media": {
    "type": "application/json;profile=https://schema.org/Rating"
  },
  "type": "object",
  "properties": {
    "author": {
      "$ref": "#/$defs/author"
    },
    "bestRating": {
      "$ref": "#/$defs/bestRating"
    },
    "ratingExplanation": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/ratingExplanation"
      }
    },
    "ratingValue": {
      "$ref": "#/$defs/ratingValue"
    },
    "reviewAspect": {
      "oneOf": [
        {
          "$ref": "#/$defs/reviewAspect"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/reviewAspect"
          }
        }
      ]
    },
    "worstRating": {
      "$ref": "#/$defs/worstRating"
    }
  },
  "$defs": {
    "arrayOfRatings": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "author": {
      "title": "Author",
      "description": "The author of this content or rating. Please note that author is special in that HTML 5 provides a special mechanism for indicating authorship via the rel tag. That is equivalent to this and may be used interchangeably.",
      "anyOf": [
        {
          "$ref": "Person.json#/$defs/possibleRef"
        },
        {
          "$ref": "Organization.json#/$defs/possibleRef"
        }
      ]
    },
    "bestRating": {
      "title": "Best Rating",
      "description": "The highest value allowed in this rating system. If bestRating is omitted, 5 is assumed.",
      "anyOf": [
        {
          "type": "number"
        },
        {
          "type": "string"
        }
      ]
    },
    "ratingExplanation": {
      "title": "Rating Explanation",
      "description": "A short explanation (e.g. one to two sentences) providing background context and other information that led to the conclusion expressed in the rating. This is particularly applicable to ratings associated with \"fact check\" markup using ClaimReview.",
      "type": "string"
    },
    "ratingValue": {
      "title": "Rating Value",
      "description": "The rating for the content.\n\nUsage guidelines:\n\n\nUse values from 0123456789 (Unicode 'DIGIT ZERO' (U+0030) to 'DIGIT NINE' (U+0039)) rather than superficially similiar Unicode symbols.\nUse '.' (Unicode 'FULL STOP' (U+002E)) rather than ',' to indicate a decimal point. Avoid using these symbols as a readability separator.",
      "anyOf": [
        {
          "type": "number"
        },
        {
          "type": "string"
        }
      ]
    },
    "reviewAspect": {
      "title": "Review Aspect",
      "description": "This Review or Rating is relevant to this part or facet of the itemReviewed.",
      "type": "string"
    },
    "worstRating": {
      "title": "Worst Rating",
      "description": "The lowest value allowed in this rating system. If worstRating is omitted, 1 is assumed.",
      "anyOf": [
        {
          "type": "number"
        },
        {
          "type": "string"
        }
      ]
    }
  }
}