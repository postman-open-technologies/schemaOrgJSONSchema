{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/Class.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Class",
  "media": {
    "type": "application/json;profile=https://schema.org/Class"
  },
  "type": "object",
  "properties": {
    "supersededBy": {
      "oneOf": [
        {
          "$ref": "#/$defs/supersededBy"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/supersededBy"
          }
        }
      ]
    }
  },
  "$defs": {
    "arrayOfClasss": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "supersededBy": {
      "title": "Superseded by",
      "description": "Relates a term (i.e. a property, class or enumeration) to one that supersedes it.",
      "anyOf": [
        {
          "$ref": "Property.json#/$defs/possibleRef"
        },
        {
          "$ref": "Class.json#/$defs/possibleRef"
        },
        {
          "$ref": "Enumeration.json#/$defs/possibleRef"
        }
      ]
    }
  }
}