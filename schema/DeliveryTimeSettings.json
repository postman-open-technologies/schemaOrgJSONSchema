{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/DeliveryTimeSettings.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Delivery Time Settings",
  "media": {
    "type": "application/json;profile=https://schema.org/DeliveryTimeSettings"
  },
  "allOf": [
    {
      "$ref": "StructuredValue.json"
    }
  ],
  "type": "object",
  "properties": {
    "deliveryTime": {
      "$ref": "ShippingDeliveryTime.json#/$defs/possibleRef"
    },
    "isUnlabelledFallback": {
      "$ref": "#/$defs/isUnlabelledFallback"
    },
    "shippingDestination": {
      "oneOf": [
        {
          "$ref": "DefinedRegion.json#/$defs/possibleRef"
        },
        {
          "type": "array",
          "items": {
            "$ref": "DefinedRegion.json#/$defs/possibleRef"
          }
        }
      ]
    },
    "transitTimeLabel": {
      "oneOf": [
        {
          "$ref": "#/$defs/transitTimeLabel"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/transitTimeLabel"
          }
        }
      ]
    }
  },
  "$defs": {
    "arrayOfDeliveryTimeSettingss": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "isUnlabelledFallback": {
      "title": "Is Unlabelled Fallback",
      "description": "This can be marked 'true' to indicate that some published DeliveryTimeSettings or ShippingRateSettings are intended to apply to all OfferShippingDetails published by the same merchant, when referenced by a shippingSettingsLink in those settings. It is not meaningful to use a 'true' value for this property alongside a transitTimeLabel (for DeliveryTimeSettings) or shippingLabel (for ShippingRateSettings), since this property is for use with unlabelled settings.",
      "type": "boolean"
    },
    "transitTimeLabel": {
      "title": "Transit Time Label",
      "description": "Label to match an OfferShippingDetails with a DeliveryTimeSettings (within the context of a shippingSettingsLink cross-reference).",
      "type": "string"
    }
  }
}