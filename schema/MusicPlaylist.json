{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/MusicPlaylist.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Music Playlist",
  "media": {
    "type": "application/json;profile=https://schema.org/MusicPlaylist"
  },
  "allOf": [
    {
      "$ref": "CreativeWork.json"
    }
  ],
  "type": "object",
  "properties": {
    "numTracks": {
      "$ref": "#/$defs/numTracks"
    },
    "track": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/track"
      }
    }
  },
  "$defs": {
    "arrayOfMusicPlaylists": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "numTracks": {
      "title": "Num Tracks",
      "description": "The number of tracks in this album or playlist.",
      "type": "integer"
    },
    "track": {
      "title": "Track",
      "description": "A music recording (track)—usually a single song. If an ItemList is given, the list should contain items of type MusicRecording.\n                 Supersedes \n                \n    \n    \n    tracks.",
      "anyOf": [
        {
          "$ref": "ItemList.json#/$defs/possibleRef"
        },
        {
          "$ref": "MusicRecording.json#/$defs/possibleRef"
        }
      ]
    }
  }
}