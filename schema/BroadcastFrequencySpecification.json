{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/BroadcastFrequencySpecification.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Broadcast Frequency Specification",
  "media": {
    "type": "application/json;profile=https://schema.org/BroadcastFrequencySpecification"
  },
  "allOf": [
    {
      "$ref": "Intangible.json"
    }
  ],
  "type": "object",
  "properties": {
    "broadcastFrequencyValue": {
      "$ref": "#/$defs/broadcastFrequencyValue"
    },
    "broadcastSignalModulation": {
      "$ref": "#/$defs/broadcastSignalModulation"
    },
    "broadcastSubChannel": {
      "$ref": "#/$defs/broadcastSubChannel"
    }
  },
  "$defs": {
    "arrayOfBroadcastFrequencySpecifications": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "broadcastFrequencyValue": {
      "title": "Broadcast Frequency Value",
      "description": "The frequency in MHz for a particular broadcast.",
      "anyOf": [
        {
          "type": "number"
        },
        {
          "$ref": "QuantitativeValue.json#/$defs/possibleRef"
        }
      ]
    },
    "broadcastSignalModulation": {
      "title": "Broadcast Signal Modulation",
      "description": "The modulation (e.g. FM, AM, etc) used by a particular broadcast service.",
      "anyOf": [
        {
          "$ref": "QualitativeValue.json#/$defs/possibleRef"
        },
        {
          "type": "string"
        }
      ]
    },
    "broadcastSubChannel": {
      "title": "Broadcast Sub Channel",
      "description": "The subchannel used for the broadcast.",
      "type": "string"
    }
  }
}