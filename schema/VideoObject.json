{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/VideoObject.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Video Object",
  "media": {
    "type": "application/json;profile=https://schema.org/VideoObject"
  },
  "type": "object",
  "properties": {
    "actor": {
      "$ref": "Person.json#/$defs/possibleRefArray"
    },
    "caption": {
      "$ref": "#/$defs/caption"
    },
    "director": {
      "$ref": "Person.json#/$defs/possibleRef"
    },
    "embeddedTextCaption": {
      "oneOf": [
        {
          "$ref": "#/$defs/embeddedTextCaption"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/embeddedTextCaption"
          }
        }
      ]
    },
    "musicBy": {
      "$ref": "#/$defs/musicBy"
    },
    "thumbnail": {
      "$ref": "ImageObject.json#/$defs/possibleRef"
    },
    "transcript": {
      "$ref": "#/$defs/transcript"
    },
    "videoFrameSize": {
      "$ref": "#/$defs/videoFrameSize"
    },
    "videoQuality": {
      "$ref": "#/$defs/videoQuality"
    }
  },
  "$defs": {
    "arrayOfVideoObjects": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "caption": {
      "title": "Caption",
      "description": "The caption for this object. For downloadable machine formats (closed caption, subtitles etc.) use MediaObject and indicate the encodingFormat.",
      "anyOf": [
        {
          "$ref": "MediaObject.json#/$defs/possibleRef"
        },
        {
          "type": "string"
        }
      ]
    },
    "embeddedTextCaption": {
      "title": "Embedded Text Caption",
      "description": "Represents textual captioning from a MediaObject, e.g. text of a 'meme'.",
      "type": "string"
    },
    "musicBy": {
      "title": "Music by",
      "description": "The composer of the soundtrack.",
      "anyOf": [
        {
          "$ref": "Person.json#/$defs/possibleRef"
        },
        {
          "$ref": "MusicGroup.json#/$defs/possibleRef"
        }
      ]
    },
    "transcript": {
      "title": "Transcript",
      "description": "If this MediaObject is an AudioObject or VideoObject, the transcript of that object.",
      "type": "string"
    },
    "videoFrameSize": {
      "title": "Video Frame Size",
      "description": "The frame size of the video.",
      "type": "string"
    },
    "videoQuality": {
      "title": "Video Quality",
      "description": "The quality of the video.",
      "type": "string"
    }
  }
}