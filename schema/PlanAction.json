{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/PlanAction.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Plan Action",
  "media": {
    "type": "application/json;profile=https://schema.org/PlanAction"
  },
  "type": "object",
  "properties": {
    "scheduledTime": {
      "$ref": "#/$defs/scheduledTime"
    }
  },
  "$defs": {
    "arrayOfPlanActions": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "scheduledTime": {
      "title": "Scheduled Time",
      "description": "The time the object is scheduled to.",
      "type": "string",
      "format": "date-time"
    }
  }
}