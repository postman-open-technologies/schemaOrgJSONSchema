{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/LodgingBusiness.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Lodging Business",
  "media": {
    "type": "application/json;profile=https://schema.org/LodgingBusiness"
  },
  "type": "object",
  "properties": {
    "amenityFeature": {
      "$ref": "LocationFeatureSpecification.json#/$defs/possibleRefArray"
    },
    "audience": {
      "$ref": "Audience.json#/$defs/possibleRef"
    },
    "availableLanguage": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/availableLanguage"
      }
    },
    "checkinTime": {
      "$ref": "#/$defs/checkinTime"
    },
    "checkoutTime": {
      "$ref": "#/$defs/checkoutTime"
    },
    "numberOfRooms": {
      "$ref": "#/$defs/numberOfRooms"
    },
    "petsAllowed": {
      "oneOf": [
        {
          "$ref": "#/$defs/petsAllowed"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/petsAllowed"
          }
        }
      ]
    },
    "starRating": {
      "$ref": "Rating.json#/$defs/possibleRefArray"
    }
  },
  "$defs": {
    "arrayOfLodgingBusinesss": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "availableLanguage": {
      "title": "Available Language",
      "description": "A language someone may use with or at the item, service or place. Please use one of the language codes from the IETF BCP 47 standard. See also inLanguage",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "$ref": "Language.json#/$defs/possibleRef"
        }
      ]
    },
    "checkinTime": {
      "title": "Checkin Time",
      "description": "The earliest someone may check into a lodging establishment.",
      "anyOf": [
        {
          "type": "string",
          "format": "time"
        },
        {
          "type": "string",
          "format": "date-time"
        }
      ]
    },
    "checkoutTime": {
      "title": "Checkout Time",
      "description": "The latest someone may check out of a lodging establishment.",
      "anyOf": [
        {
          "type": "string",
          "format": "time"
        },
        {
          "type": "string",
          "format": "date-time"
        }
      ]
    },
    "numberOfRooms": {
      "title": "Number of Rooms",
      "description": "The number of rooms (excluding bathrooms and closets) of the accommodation or lodging business.\nTypical unit code(s): ROM for room or C62 for no unit. The type of room can be put in the unitText property of the QuantitativeValue.",
      "anyOf": [
        {
          "$ref": "QuantitativeValue.json#/$defs/possibleRef"
        },
        {
          "type": "number"
        }
      ]
    },
    "petsAllowed": {
      "title": "Pets Allowed",
      "description": "Indicates whether pets are allowed to enter the accommodation or lodging business. More detailed information can be put in a text value.",
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "string"
        }
      ]
    }
  }
}