{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/DefinedRegion.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Defined Region",
  "media": {
    "type": "application/json;profile=https://schema.org/DefinedRegion"
  },
  "allOf": [
    {
      "$ref": "StructuredValue.json"
    }
  ],
  "type": "object",
  "properties": {
    "addressCountry": {
      "$ref": "#/$defs/addressCountry"
    },
    "addressRegion": {
      "$ref": "#/$defs/addressRegion"
    },
    "postalCode": {
      "$ref": "#/$defs/postalCode"
    },
    "postalCodePrefix": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/postalCodePrefix"
      }
    },
    "postalCodeRange": {
      "$ref": "PostalCodeRangeSpecification.json#/$defs/possibleRefArray"
    }
  },
  "$defs": {
    "arrayOfDefinedRegions": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "addressCountry": {
      "title": "Address Country",
      "description": "The country. For example, USA. You can also provide the two-letter ISO 3166-1 alpha-2 country code.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "$ref": "Country.json#/$defs/possibleRef"
        }
      ]
    },
    "addressRegion": {
      "title": "Address Region",
      "description": "The region in which the locality is, and which is in the country. For example, California or another appropriate first-level Administrative division",
      "type": "string"
    },
    "postalCode": {
      "title": "Postal Code",
      "description": "The postal code. For example, 94043.",
      "type": "string"
    },
    "postalCodePrefix": {
      "title": "Postal Code Prefix",
      "description": "A defined range of postal codes indicated by a common textual prefix. Used for non-numeric systems such as UK.",
      "type": "string"
    }
  }
}