{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/ScreeningEvent.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Screening Event",
  "media": {
    "type": "application/json;profile=https://schema.org/ScreeningEvent"
  },
  "allOf": [
    {
      "$ref": "Event.json"
    }
  ],
  "type": "object",
  "properties": {
    "subtitleLanguage": {
      "oneOf": [
        {
          "$ref": "#/$defs/subtitleLanguage"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/subtitleLanguage"
          }
        }
      ]
    },
    "videoFormat": {
      "$ref": "#/$defs/videoFormat"
    },
    "workPresented": {
      "$ref": "Movie.json#/$defs/possibleRef"
    }
  },
  "$defs": {
    "arrayOfScreeningEvents": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "subtitleLanguage": {
      "title": "Subtitle Language",
      "description": "Languages in which subtitles/captions are available, in IETF BCP 47 standard format.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "$ref": "Language.json#/$defs/possibleRef"
        }
      ]
    },
    "videoFormat": {
      "title": "Video Format",
      "description": "The type of screening or video broadcast used (e.g. IMAX, 3D, SD, HD, etc.).",
      "type": "string"
    }
  }
}