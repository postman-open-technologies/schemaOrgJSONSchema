{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/CreativeWorkSeries.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Creative Work Series",
  "media": {
    "type": "application/json;profile=https://schema.org/CreativeWorkSeries"
  },
  "allOf": [
    {
      "$ref": "Series.json"
    }
  ],
  "type": "object",
  "properties": {
    "endDate": {
      "$ref": "#/$defs/endDate"
    },
    "issn": {
      "$ref": "#/$defs/issn"
    },
    "startDate": {
      "$ref": "#/$defs/startDate"
    }
  },
  "$defs": {
    "arrayOfCreativeWorkSeriess": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "endDate": {
      "title": "End Date",
      "description": "The end date and time of the item (in ISO 8601 date format).",
      "type": "string",
      "format": "date-time"
    },
    "issn": {
      "title": "Issn",
      "description": "The International Standard Serial Number (ISSN) that identifies this serial publication. You can repeat this property to identify different formats of, or the linking ISSN (ISSN-L) for, this serial publication.",
      "type": "string"
    },
    "startDate": {
      "title": "Start Date",
      "description": "The start date and time of the item (in ISO 8601 date format).",
      "type": "string",
      "format": "date-time"
    }
  }
}