{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/Gene.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Gene",
  "media": {
    "type": "application/json;profile=https://schema.org/Gene"
  },
  "allOf": [
    {
      "$ref": "BioChemEntity.json"
    }
  ],
  "type": "object",
  "properties": {
    "alternativeOf": {
      "oneOf": [
        {
          "$ref": "Gene.json#/$defs/possibleRef"
        },
        {
          "type": "array",
          "items": {
            "$ref": "Gene.json#/$defs/possibleRef"
          }
        }
      ]
    },
    "encodesBioChemEntity": {
      "oneOf": [
        {
          "$ref": "BioChemEntity.json#/$defs/possibleRef"
        },
        {
          "type": "array",
          "items": {
            "$ref": "BioChemEntity.json#/$defs/possibleRef"
          }
        }
      ]
    },
    "expressedIn": {
      "oneOf": [
        {
          "$ref": "#/$defs/expressedIn"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/expressedIn"
          }
        }
      ]
    },
    "hasBioPolymerSequence": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/hasBioPolymerSequence"
      }
    }
  },
  "$defs": {
    "arrayOfGenes": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "expressedIn": {
      "title": "Expressed in",
      "description": "Tissue, organ, biological sample, etc in which activity of this gene has been observed experimentally. For example brain, digestive system.",
      "anyOf": [
        {
          "$ref": "AnatomicalStructure.json#/$defs/possibleRef"
        },
        {
          "$ref": "AnatomicalSystem.json#/$defs/possibleRef"
        },
        {
          "$ref": "BioChemEntity.json#/$defs/possibleRef"
        },
        {
          "$ref": "DefinedTerm.json#/$defs/possibleRef"
        }
      ]
    },
    "hasBioPolymerSequence": {
      "title": "Has Bio Polymer Sequence",
      "description": "A symbolic representation of a BioChemEnity. For example, a nucleotide sequence of a Gene or an amino acid sequence of a Protein.",
      "type": "string"
    }
  }
}