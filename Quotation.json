{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/Quotation.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Quotation",
  "media": {
    "type": "application/json;profile=https://schema.org/Quotation"
  },
  "type": "object",
  "properties": {
    "spokenByCharacter": {
      "$ref": "#/$defs/spokenByCharacter"
    }
  },
  "$defs": {
    "arrayOfQuotations": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "spokenByCharacter": {
      "title": "Spoken by Character",
      "description": "The (e.g. fictional) character, Person or Organization to whom the quotation is attributed within the containing CreativeWork.",
      "anyOf": [
        {
          "$ref": "Organization.json#/$defs/possibleRef"
        },
        {
          "$ref": "Person.json#/$defs/possibleRef"
        }
      ]
    }
  }
}