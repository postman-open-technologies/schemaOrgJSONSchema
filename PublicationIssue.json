{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/PublicationIssue.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Publication Issue",
  "media": {
    "type": "application/json;profile=https://schema.org/PublicationIssue"
  },
  "type": "object",
  "properties": {
    "issueNumber": {
      "oneOf": [
        {
          "$ref": "#/$defs/issueNumber"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/issueNumber"
          }
        }
      ]
    },
    "pageEnd": {
      "$ref": "#/$defs/pageEnd"
    },
    "pageStart": {
      "$ref": "#/$defs/pageStart"
    },
    "pagination": {
      "oneOf": [
        {
          "$ref": "#/$defs/pagination"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/pagination"
          }
        }
      ]
    }
  },
  "$defs": {
    "arrayOfPublicationIssues": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "issueNumber": {
      "title": "Issue Number",
      "description": "Identifies the issue of publication; for example, \"iii\" or \"2\".",
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "string"
        }
      ]
    },
    "pageEnd": {
      "title": "Page End",
      "description": "The page on which the work ends; for example \"138\" or \"xvi\".",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "integer"
        }
      ]
    },
    "pageStart": {
      "title": "Page Start",
      "description": "The page on which the work starts; for example \"135\" or \"xiii\".",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "integer"
        }
      ]
    },
    "pagination": {
      "title": "Pagination",
      "description": "Any description of pages that is not separated into pageStart and pageEnd; for example, \"1-6, 9, 55\" or \"10-12, 46-49\".",
      "type": "string"
    }
  }
}