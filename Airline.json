{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/Airline.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Airline",
  "media": {
    "type": "application/json;profile=https://schema.org/Airline"
  },
  "type": "object",
  "properties": {
    "boardingPolicy": {
      "$ref": "BoardingPolicyType.json#/$defs/possibleRef"
    },
    "iataCode": {
      "oneOf": [
        {
          "$ref": "#/$defs/iataCode"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/iataCode"
          }
        }
      ]
    }
  },
  "$defs": {
    "arrayOfAirlines": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "iataCode": {
      "title": "Iata Code",
      "description": "IATA identifier for an airline or airport.",
      "type": "string"
    }
  }
}